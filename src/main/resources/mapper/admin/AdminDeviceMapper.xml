<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cqyz.aibeijiayuan.api.admin.mapper.AdminDeviceMapper">

    <select id="getList" resultType="com.cqyz.aibeijiayuan.bean.device.vo.DeviceVO">
        select dv.*,kindergarten_name,dt.device_type_name,ag.agent_name from (abjy_tb_device dv , abjy_tb_device_type dt
        ,
        abjy_tb_kindergarten kg)
        left join abjy_tb_agent ag on ag.agent_id = dv.agent_id
        where dv.kindergarten_id = kg.kindergarten_id
        and dv.device_type_id = dt.device_type_id
        and dv.is_delete=0
        <if test="deviceMacAddress !=null and deviceMacAddress !=''">
            and dv.device_mac_address like concat('%',#{deviceMacAddress},'%')
        </if>
        <if test="deviceTypeId!=null and deviceTypeId!=0">
            and dv.device_type_id = #{deviceTypeId}
        </if>
        <if test="kindergartenId!=null and kindergartenId!=''">
            and dv.kindergarten_id = #{kindergartenId}
        </if>
        order by create_time desc
    </select>
</mapper>